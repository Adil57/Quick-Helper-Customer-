name: Flutter Android Build (APK)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    # CRITICAL FIX 1: GRADLE_OPTS ko job level par set rakhenge
    env:
      GRADLE_OPTS: "-Xmx3072m -Dorg.gradle.daemon=false" 
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17' 

      # 1. Set up Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: false 
          
      # 2. Clean and Get Dependencies
      - name: Clean and Get Dependencies
        run: |
          flutter clean
          flutter pub get
        
      # 3. Grant Execute Permission to gradlew
      - name: Grant Execute Permission to gradlew
        run: chmod +x ./gradlew
        
      # 4. 🟢 CRITICAL FIX: Final Single-Step Execution (The Guaranteed Fix)
      - name: Build Debug APK (Single Step Clean & Run)
        run: |
          echo "Applying final script cleanup and starting direct execution..."
          
          # 1. CRITICAL: Conflicting line ko delete karo
          sed -i '/DEFAULT_JVM_OPTS/d' ./gradlew
          
          # 2. **IMMEDIATELY** build chalao usi shell session mein
          ./gradlew assembleDebug
        
      # 5. SHA-1 Extraction (Optional)
      - name: Extract Debug SHA-1 Fingerprint (Final Dynamic Check)
        run: |
          echo "--- DEBUG SHA-1 FINGERPRINT (KEYTOOL - DYNAMIC) ---"
          KEYSTORE_PATH=$(find /home/runner/ -name "debug.keystore" 2>/dev/null | head -n 1)
          if [ -f "$KEYSTORE_PATH" ]; then
            echo "Keystore found at: $KEYSTORE_PATH"
            /usr/bin/keytool -list -v -keystore "$KEYSTORE_PATH" \
            -alias androiddebugkey -storepass android -keypass android
          else
            echo "Error: debug.keystore not found anywhere! Could not extract SHA-1."
          fi
          echo "---------------------------------------------------"
        
      # 6. Build Android Release APK (Single Step Clean & Run)
      - name: Build Android Release APK
        run: |
          # Conflicting line ko delete karo
          sed -i '/DEFAULT_JVM_OPTS/d' ./gradlew
          
          # IMMEDIATELY build chalao usi shell session mein
          ./gradlew assembleRelease
        
      - name: Upload Artifact (APK)
        uses: actions/upload-artifact@v4 
        with:
          name: app-release-apk
          path: build/app/outputs/apk/release/app-release.apk
          
      - name: Notify on Success
        if: success()
        run: echo "APK build successful! Check Artifacts tab."
```eof

Bhai, aap yeh **latest YAML** use karo. **`sed` aur `./gradlew` ko combine** karna hi is **shell execution timing bug** ka aakhri ilaaj hai. Aapki mehnat zaroor rang laayegi! Please try karo!

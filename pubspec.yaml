name: quick_helper_customer
description: Customer App for booking helper services.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0' 

dependencies:
  flutter:
    sdk: flutter
  
  # 🟢 AUTH & API
  auth0_flutter: ^1.14.0 
  http: ^1.2.1 
  
  # 🟢 MAP SYSTEM (MapBox)
  mapbox_maps_flutter: ^2.17.0  
  
  # 🟢 GPS/LOCATION FIX (Zaroori packages for runtime permission and coordinates)
  permission_handler: ^11.3.1 # Run-time permission maangne ke liye
  geolocator: ^11.0.0 # Actual GPS coordinates fetch karne ke liye
  
  # UI/Icons
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^10.6.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  

# pubspec.yaml (Final Firebase Setup)

name: quick_helper_customer
description: Customer App for booking helper services.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management (Simple)
  provider: ^6.0.5 # Tumne yeh add kiya hai, isko use bhi karna chahiye.

  # Firebase Core Modules (Authentication and Database)
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.2
  cloud_firestore: ^4.13.3 # Firestore for helper/booking data
  
  # UI/Icons
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^10.6.0 # Tumne yeh add kiya hai
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  

# pubspec.yaml (Fixed)

name: quick_helper_customer
description: Customer App for booking helper services.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0' 

dependencies:
  flutter:
    sdk: flutter
  
  # 🟢 AUTH & API
  auth0_flutter: ^1.14.0 
  http: ^1.2.1 
  
  # 🟢 MAP SYSTEM (MapBox - No more latlong2 issue)
  # Latlong2 aur flutter_map hata diye gaye hain
  mapbox_maps_flutter: ^1.1.0  # Mapbox SDK replaces flutter_map
  location: ^8.0.0           # Location utility package rehne diya
  
  # UI/Icons
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^10.6.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  

name: quick_helper_customer
description: Customer App for booking helper services.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0' 

dependencies:
  flutter:
    sdk: flutter
  
  # 🟢 AUTH & API
  auth0_flutter: ^1.14.0 
  http: ^1.2.1 
  shared_preferences: ^2.3.5 # 👈 Persistent login ke liye add kiya
  
  # 🟢 MAP SYSTEM (MapBox)
  mapbox_maps_flutter: ^2.17.0  
  
  # 🟢 GPS/LOCATION FIX
  permission_handler: ^11.3.1 
  geolocator: ^11.0.0 
  
  # UI/Icons
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^10.6.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  

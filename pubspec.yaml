# pubspec.yaml

dependencies:
  flutter:
    sdk: flutter
  
  # 🟢 New Official Auth0 SDK
  auth0_flutter: ^3.0.0 
  
  # For making API calls to Render/MongoDB server
  http: ^1.2.1 
  
  # UI/Icons
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^10.6.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  

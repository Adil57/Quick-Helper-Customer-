# pubspec.yaml

name: quick_helper_customer
description: Customer App for booking helper services.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0' 

dependencies:
  flutter:
    sdk: flutter
  
  # 🟢 AUTH & API
  auth0_flutter: ^1.14.0 
  http: ^1.2.1 
  
  # 🟢 FREE MAP SYSTEM (OpenStreetMap) - LATEST STABLE
  # Yeh latest versions hain (December 2025 tak ke hisaab se)
  flutter_map: ^8.2.2        # Latest stable version
  latlong2: ^0.9.1           # Iski version yahi latest hai
  location: ^8.0.0           # Latest stable version
  
  # UI/Icons
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^10.6.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  

# pubspec.yaml

name: quick_helper_customer
description: Customer App for booking helper services.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 🟢 AUTHENTICATION
  auth0_flutter: ^1.14.0 
  
  # 🟢 API CALLS
  http: ^1.2.1 
  
  # 🟢 MAP SYSTEM (Live Location & Markers)
  google_maps_flutter: ^2.5.0 # Live Map widget
  location: ^5.0.3           # User ki current location access karne ke liye
  
  # UI/Icons
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^10.6.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  

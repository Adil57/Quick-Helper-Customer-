// File: android/build.gradle (Groovy DSL) - FINAL FINAL VERSION

// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        def kotlinVersion = "1.8.20" 
        
        classpath 'com.android.tools.build:gradle:8.0.0' 
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
    }
}

// Tumhari build directory configuration:
def newBuildDir = rootProject.layout.buildDirectory
        .dir("../../build")
        .get()
rootProject.layout.buildDirectory.set(newBuildDir)

subprojects {
    def newSubprojectBuildDir = newBuildDir.dir(project.name)
    project.layout.buildDirectory.set(newSubprojectBuildDir)
}
subprojects {
    project.evaluationDependsOn(':app')
}

// 🌟 FINAL FIX: MAPBOX REPOSITORY
allprojects {
    repositories {
        google()
        mavenCentral()
        
        maven {
            url "https://api.mapbox.com/downloads/v2/releases/maven"
            
            // ✅ Syntax Fix: Using 'create' method for Basic Auth (solves the compiler issue)
            authentication {
                create('basic', BasicAuthentication) 
            }
            
            credentials {
                username = "mapbox" 
                // ✅ Token Fix: Reads from project properties (which includes gradle.properties)
                // This is the correct way for CI when using injected properties/files.
                password = project.properties["MAPBOX_DOWNLOADS_TOKEN"]
            }
        }
    }
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
